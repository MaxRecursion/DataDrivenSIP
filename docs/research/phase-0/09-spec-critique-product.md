> Incremental notes from a research agent that was cut off by a usage limit before its final summary. Findings marked VERIFIED were checked; treat the rest as leads.

# critic-product REPORT (Phase 0, adversarial product/UX/copy/compliance/a11y read)
Status: COMPLETE (itemized list at bottom).

## Verified computations (node, scratchpad/critic-product/contrast.mjs, WCAG 2.x relative luminance)
- white(#FFFFFF) on marigold #F2A71B = 2.04:1 -> FAILS AA normal (4.5) AND AA large (3.0). Also fails 3:1 non-text vs white grid bg.
- ink #17203A on marigold = 7.91:1 -> passes AAA. => answer numeral must be ink, not white.
- white on teal #0E7C7B = 5.01:1 -> passes AA normal. ink on teal = 3.21:1 -> AA-large only.
- mute #7C8699 on surface #EEF0F4 = 3.21:1, on raised = 3.67:1 -> FAILS AA for normal-size text (labels/metadata/footer/navAsOf). Nearest passing mix toward ink: #626B80 (4.68 on surface, 5.34 on raised).
- teal on surface = 4.40:1 (fails AA normal by a hair), teal on raised = 5.01:1.
- marigold vs raised 2.04:1, vs surface 1.78:1, vs teal 2.46:1 -> marigold fill alone fails 1.4.11 non-text 3:1 as the only indicator of the answer cell; needs ink ring/border/size/label.
- raised (#FFF) vs surface = 1.14:1 -> grid/cards have no perceivable boundary without a border/shadow (shadow is static CSS, fine; animating it is forbidden).
- mute text on teal = 1.37:1, on marigold = 1.80:1 -> never put mute labels inside colored cells.
- Kotak example: 164 instalments x Rs10,000 = Rs16.4 L; 123187/7530112 (date-1 corpus from example) = 1.636% (matches "1.6%"); 123187/1640000 = 7.51% of invested. navFrom->navTo = 13.696 y (matches "13.7 years") but meaningful copy says "13 years".
- Jan-2013..Sep-2026 inclusive = 165 months; with navTo 2026-09-11, dates 1-11 can get a Sep-2026 instalment, dates 12-28 cannot -> instalmentCount likely differs by date (see F-list).
- Safe-window "wrap mod 28": naive (s+k)%28 yields 0 for s=17,20,25,26 -> must be 1-based ((s+k-1)%28)+1. Default window 3-12 equals formula at s=1 or s=29, NOT s=28/30/31.

## VERIFIED: rupee spread / metricsAgree polluted by instalment-count edge (reused spike-c3/out2.txt, Kotak 119775, opts monthSet=perDateTargetInRange look=7 term=navTo)
- Per-date instalments: d1,d2 = 164; d3-d11 = 165; d12-d28 = 164. Spike reproduces spec's spreadRupees exactly (123186.74) and spreadPp 0.1136.
- max corpus = d3 (7,575,730; 165 inst), min corpus = d2 (7,452,544; 164 inst). max XIRR = d26 20.3893, min d9 20.2757.
- => spec's "Rs1,23,187" compares dates with different instalment counts/months. Within the 165 group (d3-11, same months) spread = Rs38,824.
- => metricsAgree=false for the spec example is (at least partly) an edge artifact, not "strong evidence of noise".
- Avg corpus per instalment ~Rs45.8k -> spec's universal claim "one bounced instalment costs more than the entire date spread" is FALSE for Kotak with the spec's own number (123k > 45.8k), true-ish only with a matched month set.
- "1.6% of final corpus": 123187 / max corpus 1.626%, / min 1.653% (rounds to 1.7%), / spec's d1 7,530,112 = 1.636%. Denominator choice changes the printed digit.
- Spec example d1 corpus 7,530,112 != spike d1 7,458,455 (diff 422 units ~ one instalment) -> spec example numbers not self-consistent under this convention (cross-lane: critic-eng/analysis).

## VERIFIED: formatting (node 22.22.2, ICU 78.3)
- Intl en-IN: 1,23,187 / 16,40,000 / 1,23,45,678; currency -> "₹1,23,187".
- compact short en-IN -> "16.4L", "75.3L", "1.2Cr", "95K" (no space, SR reads letter); compact long -> "16.4 lakh", "1.2 crore".
- Ordinals via Intl.PluralRules('en-US',{type:'ordinal'}) correct for 1..28 (1st 2nd 3rd 11th 12th 13th 21st 22nd 23rd).
- DateTimeFormat en-IN short month -> "11 Sept 2026" (4-letter "Sept"): pin format explicitly or use long month.
- Spec schema funds/{code}.json with realistic Kotak values: raw 2,195 B (> "under 2 KB"; §3 says ~1.5 KB), gzip 724 B. Adding per-date wins + instalments: raw 2,671 B, gzip 779 B. Budget unit (raw vs gzip) is ambiguous.

## VERIFIED: search/name data (data-probe/mf_list.json 37,882 schemes; est_survivors.json = 1,182-row liveness sample, not full universe)
- Name regex filter alone keeps 4,723 schemes (before liveness/36-month rules).
- Survivor sample: name length p50 56, p90 76, max 120 chars ("HSBC Income Plus Arbitrage Active FOF (erstwhile HSBC Managed Solution India - Conservative Plan) - Direct Plan - Growth"). Exceeds 80-char line rule; wraps to 3+ lines at 360 px.
- 10 distinct "Direct ... Growth" suffix spellings; 125/1,182 contain "GROWTH"/"DIRECT PLAN" in caps (e.g. Canara Robeco "... - Direct Plan - GROWTH OPTION"); fully uppercase house-style names exist ("BANK OF INDIA MID CAP TAX FUND SERIES 1 ..."). Conflicts with "sentence case throughout" unless names are exempt.
- 9 groups of identical/near-identical names after stripping plan words (Nippon India "... (Existing number of Segregated Portfolios - 1) ..." appears twice) -> typeahead shows indistinguishable rows.
- schemeType in sample: Open Ended 1,151; Close Ended 30; Interval 1. Eligibility (§4.2) does not exclude closed-ended/interval/FMP/"Series" schemes -> app would give a SIP date for schemes that cannot accept SIPs.
- "kotak mid" matches two survivors: 119775 Kotak Mid Cap Fund and 120158 Kotak Large & Mid Cap Fund -> criterion 1 depends on fuzzy ranking putting 119775 first.
- 40 fund houses in sample; categories 81 distinct strings with inconsistent prefixes ("Equity Scheme -" vs "Equity Schemes -").

## ITEMIZED FINDINGS (id | section | issue | why | resolution | tag). Blockers first.
### BLOCKERS
- B1 §5.3/5.4/6.4 | Rupee spread + metricsAgree compare dates with unequal instalment counts (Kotak: d3 165 inst = max corpus, d2 164 = min; spec's 1,23,187 reproduced exactly) | headline rupee figure overstates date impact ~3-4x (same-month spreads Rs38,824 d3-11 / Rs32,530 d12-28); "metricsAgree=false => noise" becomes a window-edge artifact; "Rs16.4 L invested" wrong for 165-inst dates | simulate all 28 dates on one common month set (equal instalmentCount); re-verify criterion 3 tolerance (UNVERIFIED shift); spec example Rs figure will change | NEEDS-USER-DECISION (+critic-eng)
- B2 §6.3/6.5.3 | "One bounced instalment costs more than the entire date spread" asserted universally; "cost of one missed instalment" undefined | false for spec's own example (avg instalment value today ~Rs45.8k < Rs1,23,187) | define missedCost in pipeline (e.g. median-month instalment's value today for answer date); branch copy on missedCost vs spreadRupees; never hardcode | NEEDS-USER-DECISION
- B3 §6.4 | "+0.31 pp of XIRR" has no baseline; verdict/spreadPp are global 1-28 but answer is window+meanPct pick (Kotak default window 3-12: max XIRR in window 12th 20.3186 vs global 26th 20.3893) | "real advantage" can be false for the displayed date | define edgePp = xirr(answer) - median xirr of window candidates (precomputed or trivial runtime subtraction, confirm allowed); show meaningful copy only if edge>0, else marginal copy; one years formatter (13.7 not "13") | NEEDS-USER-DECISION
- B4 §13.5/5.4 | "reduced-confidence" undefined (no verdict value, field, UI); 36-39 mo funds have ~0-4 3y windows; meanPct undefined at 0 windows breaks selection | criterion 5 untestable; possible crash | add windows+months to artifact; reducedConfidence flag (e.g. windows<12 or months<60) shown as notice, verdict/thresholds untouched; fallback rank by window XIRR when windows==0 | NEEDS-USER-DECISION
- B5 §6.5.2/5.6 | "rolling-window winner counts" not in schema (topQ != wins); spec-schema file already 2,195 B raw (>2 KB), 2,671 B with wins+per-date inst; gzip 724/779 B | schema must freeze before Phase 3 | add per-date wins + fund windows; declare budget as gzip | NEEDS-USER-DECISION
- B6 §6.2/12 | Safe-window formula ambiguous: naive %28 gives 0 (s=17,20,25,26); salary domain (1-31? "last"?); default 3-12 == formula at s=1/29 not s=28 (URL example salary=28 -> 2-11); buffer semantics (width fixed 10? shifts default?); wrapped windows show next-month 1st-3rd top-left | NON-NEGOTIABLE "never outside safe window" rests on it | salary in {last,1..31}; window=[s+b, s+b+9] on 1-based 28-circle; last => (1+b)..(10+b); buffer 0-5; copy "1st of the next month" for wrapped days | NEEDS-USER-DECISION
- B7 §8.2/9/13.1 | "<150 ms to rendered answer" vs answer cell landing t=400-440 ms and count-up; opacity:0 staggered cells are not LCP candidates (delays LCP on cold load) | criterion 1/LCP contradiction | answer sentence non-animated at t=0; measure Enter keydown -> sentence visible; cold deep link/back-forward render final state (no morph source exists) | NEEDS-USER-DECISION
- B8 §7.2 | Contrast (verified): white/marigold 2.04; marigold vs raised 2.04, surface 1.78, teal 2.46; mute on surface 3.21/raised 3.67; teal text on surface 4.40 | answer cell fails 1.4.11/1.4.1; disclaimer+navAsOf in mute fail AA (compliance text) | ink numeral on marigold (7.91); 2px ink ring + "Your date" label; white numerals on teal (5.01); text-mute #626B80 (4.68/5.34), keep #7C8699 for non-text; teal text only on raised | NEEDS-USER-DECISION (token change)
- B9 §10/11 | immutable cache on unhashed /data/funds/{code}.json rebuilt nightly | stale answers indefinitely; fund navTo vs meta navAsOf mismatch; freshness requirement defeated | versioned data path via meta.json or short revalidating cache | NEEDS-USER-DECISION (+hosting)
### HIGH
- H1 §6.4 noise copy implies proximity rationale ("cleanest fit after your salary lands") but pick is top meanPct (may be 12th); "performs the same" present/forward tense; lacks "tiebreak" framing required by §6.3.4 | propose "Every date in your window has done about the same in this fund. The 7th is a tiebreak: it held up slightly more often across 3-year stretches." | NEEDS-USER-DECISION
- H2 §6.4 marginal = big-spread/unstable OR stable/tiny-spread; "edges ahead" misreads unstable case | two sub-copies keyed on which condition held | DESIGNER-DEFAULT
- H3 §6.4 rupee sentence: notional Rs10,000/month unstated (users read as own money; scales linearly); "best vs worst" by corpus != XIRR best elsewhere; "1.6% of final corpus" denominator (1.626 max / 1.653->1.7 min / 1.636 spec d1); mixed "Rs1,23,187" + "16.4 L" | "For a Rs10,000 monthly SIP, the highest- and lowest-value dates (the 3rd and 2nd) ended Rs1,23,187 apart on Rs16.4 lakh invested, 1.6% of the higher one's final value, over 13.7 years." | NEEDS-USER-DECISION (denominator, naming)
- H4 fonts (sibling fonts/REPORT.md, verified there): Cabinet Grotesk has NO tnum/pnum (tabular-nums no-op) -> grid/date count-up jitters; Satoshi lacks U+20B9 and U+00A0 -> every Rs in body falls back | "tabular figures everywhere" unmeetable in headline font | fixed-size cells, count-up confined to cell or dropped; Rs via Cabinet span/unicode-range fallback; no nbsp in Satoshi text | NEEDS-USER-DECISION
- H5 §13.4 "salary change visibly changes the recommended date" not universally true (overlapping windows share top date); "recommended" is advice-ish | test with disjoint windows (last vs 15th); assert window always moves | NEEDS-USER-DECISION
- H6 §4.2 eligibility keeps closed-ended/interval/FMP/"Series" Direct Growth (sample 30 closed + 1 interval of 1,182) | app gives SIP dates for schemes that can't take SIPs | exclude non-open-ended (schemeType availability in pipeline source UNVERIFIED) or show "doesn't accept new SIPs" | NEEDS-USER-DECISION
- H7 §6.1 lazy index + 120 ms debounce + "Enter selects top hit" race; cmdk highlighted vs top hit; "kotak mid" also matches 120158 Kotak Large & Mid Cap | criterion 1 flake | Enter selects highlighted (default top), queued until results for final query; loading row; score token-start on name, ignore Direct/Plan/Growth/Option/Fund tokens | DESIGNER-DEFAULT
- H8 §11 advice-edging words in spec leak risk ("recommended date", "best date", "best vs worst", og/title) | sibling SEBI read: public free non-personalised tool outside IA def; risk rises with "for you"/recommendation framing | CLAUDE.md lexicon: ban recommend(ed)/best/top fund/should/will/guaranteed/outperform in UI; past tense "has done"; "highest/lowest" for dates; no XIRR in search results, title, og; results ordered by text relevance only | DESIGNER-DEFAULT
- H9 §6.5.1 zoomed y-axis visually exaggerates a 0.11 pp spread | contradicts honesty | annotate span ("top to bottom of this chart is 0.11 percentage points"), mean line, data-table alternative, label global max outside window ("26th was highest overall but falls before your salary lands") | DESIGNER-DEFAULT
- H10 §6.5.2 confidence numbers lack baselines: topQ 0.21 < chance 0.25; meanPct chance ~50; Spearman -1..1 opaque; meaningful+metricsAgree=false contradictory, no precedence | plain words + chance baselines; caveat sentence under headline when meaningful && !metricsAgree | NEEDS-USER-DECISION (precedence)
- H11 §7.1 landing with no fund undefined; morph target conflicts if landing has a grid | NEEDS-USER-DECISION; default: H1 question + search + neutral grid with default window 3-12 lit, no marigold, caption, one-line honesty promise, no demo fund answer
- H12 §6.6 client-side og/title invisible to WhatsApp/X/LinkedIn/Slack crawlers (no JS) | shared links preview generic | accept for v1 or emit static per-fund HTML shells at build (no SSR runtime); og text without date/advice | NEEDS-USER-DECISION
- H13 answer churn across nightly builds (near-tie meanPct) + no tiebreak rule | returning/shared-link users see a different date unexplained | deterministic tiebreak (topQ, xirr, earliest after salary); "Based on NAVs up to ..." under answer; noise copy: dates in window interchangeable (no reason implied to switch an existing SIP); hysteresis NEEDS-USER-DECISION
- H14 not-found/removed/merged/renamed: stale artifacts, Regular/IDCW/<36-mo searches return nothing; SPA fallback may return 200 HTML for missing JSON (UNVERIFIED CF behaviour) | states: not covered (explain coverage), HTML/404/parse -> not-found, navTo stale -> "stopped publishing NAVs on X, may be merged", rename tip "try fund house name"; pipeline prune vs tombstone | NEEDS-USER-DECISION (prune/tombstone)
- H15 a11y flow: focus lost when result morphs; 28 announced cells; tickers in live regions; "pp"/"L"/XIRR poorly read | focus answer heading (tabindex -1); grid aria-hidden + sr-only "Your window: 3rd to 12th. Your date: the 7th."; no role=grid unless interactive; animated numbers aria-hidden with final sr-only; one polite live region for salary changes; lang=en-IN; SR reading of "1,23,187" UNVERIFIED | DESIGNER-DEFAULT
- H16 "/" shortcut: WCAG 2.1.4 (A) needs off/remap/focus-only; conflicts Firefox Quick Find, inputs, mobile | ignore in editable/select/modifiers, preventDefault, hint only pointer:fine; off switch (localStorage) or accept risk | NEEDS-USER-DECISION
### MEDIUM
- M1 result rows: identical names (9 groups), long names (p90 76, max 120) -> 2-line rows, house + category on line 2 (no middle dots), AMFI code for identical names | DESIGNER-DEFAULT
- M2 sentence case vs verbatim names ("GROWTH OPTION" 125/1,182; 10 suffix variants; inconsistent category prefixes) -> names verbatim (exempt), category display map | DESIGNER-DEFAULT
- M3 formatting: en-IN grouping; prose "Rs16.4 lakh"/"Rs1.02 crore" (not compact "16.4L"/"1.2Cr"); pp 2 dp with "<0.01" floor; XIRR 2 dp; U+2212 minus; ordinals via PluralRules; dates "11 September 2026" (en-IN short gives "Sept"); years 1 dp from navFrom-navTo; ticker reserves final width (Indian grouping shifts comma positions) | DESIGNER-DEFAULT
- M4 salary Select (Last working day, 1st..31st) below answer; 360x640 answer may be off-screen when it changes -> inline echo + live region; replaceState for salary/buffer, pushState for fund; invalid params clamp + canonical; omit defaults; "advanced options" = collapsed "Buffer days" control beside salary | DESIGNER-DEFAULT
- M5 orchestration matrix: select=full sequence; cold/back-forward=static; salary change=teal layer opacity crossfade + ring transform, no haptic/ticker (spread unchanged); interrupt=jump to final; reduced motion=static+no vibrate; "teal fill" at 260 ms must be opacity layer (background-color is paint); vibrate needs user activation (UNVERIFIED console) | DESIGNER-DEFAULT
- M6 tone: haptic+spring+count-up celebrates "noise"; count-up passes through wrong dates | same sequence all verdicts; count-up NEEDS-USER-DECISION
- M7 shadcn vs spec: Accordion/Collapsible animate height (accordion-down keyframes); Button transition-all; tracking-widest shortcuts; token mapping (--primary ink, --ring teal/ink, --muted-foreground #626B80, distinct --destructive, --border since raised/surface 1.14:1); Radix unmounts closed content (content-visibility moot; hidden=until-found); color-scheme light; forced-colors borders (class strings from memory, UNVERIFIED) | DESIGNER-DEFAULT
- M8 360 px: "4x7" ambiguous. 7 cols x 4 rows -> 41.7 px cells (1.06 scale = 44.2 px fits 6 px gaps), grid ~185 px tall; 4 cols x 7 rows -> 77.5 px cells, ~578 px tall, answer below fold. Mute numerals need >=24 px. uPlot 28 bars ~11.7 px slots -> labels 1,7,14,21,28; search input >=16 px | NEEDS-USER-DECISION (orientation)
- M9 freshness: fund navTo near answer + meta navAsOf in footer; stale banner threshold (data.yml only guards count drop); meta.json lacks source -> hardcoded "via mfapi.in" breaks on AMFI swap | DESIGNER-DEFAULT (+threshold NEEDS-USER-DECISION)
- M10 AMFI ToU "personal and non-commercial use", no public display/derivatives (sibling fonts/REPORT.md §6; UNVERIFIED by me) vs footer naming AMFI | NEEDS-USER-DECISION (legal)
- M11 state inventory: index loading/fail/no-results; fund JSON loading (neutral grid placeholder, zero CLS)/404/parse; meta fail; offline; chart chunk fail; noscript disclaimer | DESIGNER-DEFAULT
- M12 edge values: negative XIRR, 0.00 pp, debt/liquid funds, 245-month funds (crore) | DESIGNER-DEFAULT
- M13 "the UI says this explicitly" (never outside window) has no copy/location | put in "What actually matters" using computed numbers | DESIGNER-DEFAULT
- M14 prefetch on hover impossible on touch -> prefetch top hit whenever results update | DESIGNER-DEFAULT
- M15 existing-SIP holders' question ("should I switch?") unaddressed; answering = advice risk | factual noise line only | NEEDS-USER-DECISION
- M16 body line length: rupee sentence ~110+ chars -> max-width ~65ch | DESIGNER-DEFAULT
