> Incremental notes from a research agent that was cut off by a usage limit before its final summary. Findings marked VERIFIED were checked; treat the rest as leads.

# critic-eng REPORT (adversarial engineering read of spec.md) — incremental
Started 2026-09-15. Method: spec reasoning + in-memory node checks (no web, no sibling files read).

## Verified numeric checks (node v22.22.2 on this Mac, in-memory; no data files)
- V1 Artifact size, spec §5.6 row-of-objects shape, 28 dates, 3-dp floats: raw 2,260–2,319 B (name 41–100 chars), gzip-9 ~763 B. Columnar (xirr[],corpus[],meanPct[],topQ[]): raw 1,124–1,183 B, gzip ~620–626 B. => "under 2 KB" FAILS raw with example shape; "~1.5 KB" (§3) only met by columnar.
- V2 Pipeline CPU, literal bisection (100 iters, Math.pow), synthetic 165-month fund: 3,696 XIRR solves/fund (28 full + 28x129 windows + 56 halves), 242 ms/fund single-thread -> 18.5 min for 4,600 funds on THIS Mac, before any network. Early-stop at bracket width 1e-12: 104 ms/fund (8.0 min), result diff 7.6e-14. Bisection from [-0.99,3] degenerates in float64 after 55 iterations (iters 56-100 are no-ops).
- V3 Spring {k400,c30,m0.8}: zeta 0.839; 0.8->1.0 peaks at 1.0016 (t=258 ms), settles (±0.002) ~194 ms. 1.06 overshoot needs zeta 0.358 (damping ~12.8). => §8.1 rule 4 params cannot produce §8.2 "0.8 -> 1.06 -> 1.0".
- V4 Stagger: last of 28 cells starts 60+27*8 = 276 ms (after 260 ms safe-window wave start); scale 0.96->1 spring settles ~178 ms -> last cell settled ~454 ms.
- V5 Contrast (WCAG): mute/surface 3.21, mute/raised 3.67 (fail AA 4.5 small text); white/marigold 2.04 (fail); ink/marigold 7.91 ok; white/teal 5.01 ok; ink/teal 3.21 (fail small text).
- V6 Kotak example consistency: Jan-2013..Aug-2026 = 164 months = example "instalments":164; navFrom->navTo 13.69 y = "13.7 years"; 164x10k = "16.4 L". So example excludes Sep-2026 even though dates 1-11 of Sep <= navTo 2026-09-11 => implies a COMMON instalment calendar (partial last month dropped), not per-date navOnOrAfter skipping.
- V7 Salary wrap: naive (s+2)%28 gives 0 for s=26 (invalid date). 1-indexed wrap ((x-1)%28)+1 gives s=28 -> window 2..11, but default "last working day" -> 3..12: inconsistent.
- V8 Percentile: (r-1)/27 -> best 100, worst 0, top-quartile = 7 ranks; r/28 -> worst 3.57, pct>=75 = 8 ranks (topQ definition changes with formula).
- V9 Early-month corpus bias: d=1 invests 27 days before d=28 each month; at 20% p.a. that is +1.358% corpus = ~Rs 1,02,245 on Rs 75.3 L — ~83% of the example spreadRupees Rs 1,23,187 is mechanical time-in-market, not date "luck".
- V10 FV of one missed Rs 10k instalment at 20%: 13.7 y -> Rs 1,21,558; 6.85 y -> Rs 34,865. Kotak spreadRupees Rs 1,23,187 >= either => §6.3 claim "one bounced instalment costs more than the entire date spread" is FALSE in rupee-FV terms for the spec's own flagship fund.
- V11 Same literal 100-iteration bisection with NPV scaled to terminal date and exp(dt*log1p(r)): 53.9 ms/fund -> 4.1 min/4,600 funds single-thread on this Mac (10 cores); root identical (diff 5.6e-17). GH runner speed factor UNVERIFIED.
- V12 Non-bracketing: 36 instalments valued at 1% of invested -> NPV(-0.99) = -1.79e10 and NPV(3) = -9.0e4 (same sign); literal bisection silently returns 3.0 (+300%) for a >99% loss.

## BLOCKERS (resolve in PLAN.md before Phase 2/3)
B1 §5.3/5.4/§13.3 [USER] Conventions under-specified yet criterion 3 is an oracle over them: instalment calendar (first/last month, per-date null skips), window valuation date, split-half rule, percentile/ties, "7 days" inclusivity. Kotak stability 0.54 vs 0.60 threshold -> split convention decides verdict => criterion 3. Fix: obtain user's verification script/conventions; else adopt defaults (common calendar, valuation=navOnOrAfter(1st of month after last instalment month), halves by instalment months) and pin a 119775 fixture.
B2 §13.3 [USER] Criterion 3 values drift nightly (navTo moves; best/worst are noise and can flip). Fix: commit NAV snapshot truncated at an agreed cutoff (example navTo 2026-09-11) + numeric tolerances (default |dXIRR|<=0.01pp, |dSpread|<=0.002pp).
B3 §10/§5.6/§9 [USER] "immutable long cache on /data/funds/*" + same filenames rewritten nightly = users pinned to stale data for max-age. Fix: versioned path (/data/{dataVersion}/funds/{code}.json, version from tiny no-cache meta.json, also used on cold deep link) or short max-age+ETag.
B4 §3/§10 [USER] Nightly git churn: nearly every fund file changes daily (navTo, xirr, corpus) ~4.6k x 2.3 KB = ~10.6 MB raw/night (~3.5 MB gz) -> ~1+ GB/yr of history (estimate); Pages clones repo per build. Options: force-pushed orphan data branch; monthly valuation cadence; Direct Upload (needs Cloudflare token secret, conflicts "no secrets").
B5 §4.2/§5.4/§6.3/§13.5 [USER] 36-37 month funds have 0 rolling windows -> meanPct undefined -> selection undefined (crash). 40 months -> <=4 windows. Verdict enum has no "reduced confidence". Fix: add windows count + confidence flag (thresholds untouched), fallback selection when windows==0, or raise eligibility.
B6 §5.2 [ENG] Non-bracketing unspecified; literal code returns bound (V12). Fix: check signs at both ends, return null, quarantine fund + log.
B7 §4.3/§5/§13.10 [ENG] 20-min cold run: CPU alone 18.5 min naive on this Mac (V2) + network over ALL name-matched candidates. Fix: terminal-scaled NPV (V11), worker_threads, dead-scheme prefilter via one NAVAll.txt fetch.
B8 §13.1 vs §6.1/§8.2/§9 [USER] Criterion 1 omits "warm cache", conflicts with 120 ms debounce + lazy index.json + numeral count-up starting 440 ms. Fix: t0=Enter keydown, t1=final date text committed to DOM/a11y tree; Enter flushes debounce; data warm.
B9 §8.1 r2 vs r5 [USER] layoutId needs Motion's domMax; domAnimation excludes layout animations -> morph silently no-ops. "~6 KB" is only initial m+LazyMotion. Fix: LazyMotion strict with async domMax preloaded on idle (counts toward 180 KB), or hand FLIP via WAAPI.
B10 §8.2 vs §8.1 r1/§12 [ENG] "teal fill", "marigold fill", "subtle ring" = background/box-shadow paint. Fix (no weakening): pre-painted overlay layers animated by opacity/scale; numeral colour via cross-faded stacked numerals.
B11 §8.1 r4 vs §8.2 [USER] Spec spring peaks 1.0016, not 1.06 (V3). Choose: drop 1.06, or separate spring damping ~12.8 for answer cell.
B12 §13.7/§8.6 [USER] "no frame over 8.33 ms" has no environment/metric; headless CI isn't 120 Hz; 4x throttle likely fails layoutId measure frame. Fix: CDP trace on user's 120 Hz device at 1x as gate, 4x informational; define metric (main-thread per-frame work + dropped frames 0-700 ms).
B13 §9/§6.6 [USER] LCP 1.2 s "simulated Fast 3G" for CSR SPA deep link (HTML->JS->JSON->font) very likely unmeetable; throttling profile name ambiguous (Lighthouse default vs DevTools preset, UNVERIFIED). Fix: build-time static shell prerender (not SSR runtime), font preload, instant final state on cold deep link; define URL set.
B14 §6.6 [USER] og: tags set client-side are invisible to link-preview crawlers (no JS). Real previews need build-time per-fund HTML (~4.6k files); Pages Functions = backend (NON-NEG).
B15 §6.2 [USER] Salary math broken/undefined: (s+2)%28=0 for s=26; s=28 -> 2..11 vs LWD 3..12; salary 29-31 domain; buffer shifts start or whole window; buffer>=10 with fixed end -> empty window; mod-28 wrap ignores 29-31 calendar days.
B16 §6.3/§6.5/§5.4 [USER] Honesty: "one bounced instalment costs more than the entire date spread" false for Kotak in FV terms (V10); spreadRupees ~83% mechanical early-month time-in-market (V9) -> metricsAgree=false near-universal in rising funds (can co-occur with "meaningful"). Define missed-instalment cost + spread basis before copy ships.
B17 §4.3 [ENG] Cache "keyed by latest NAV date" is circular (must fetch to learn key) and every key changes each business day -> every nightly run is a cold run. Fix: cheap latest-date source then incremental append; periodic full refresh.
